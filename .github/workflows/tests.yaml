name: tests

on: pull_request

jobs:
  test:
    name: Test Jest Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.11.0'

      - run: npm install
      - run: npm test
